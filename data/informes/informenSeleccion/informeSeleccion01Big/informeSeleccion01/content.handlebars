<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Informe de Candidato</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @page {
            margin: 2cm 1.5cm;
            size: A4;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 11pt;
            line-height: 1.4;
            color: #2c3e50;
            background: #fff;
        }

        /* Header principal */
        .header {
            border-bottom: 3px solid #3498db;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .logo-area {
            text-align: center;
            margin-bottom: 15px;
        }

        .company-name {
            font-size: 24pt;
            font-weight: 700;
            color: #2c3e50;
            letter-spacing: -0.5px;
        }

        .report-title {
            font-size: 16pt;
            color: #7f8c8d;
            margin-top: 5px;
        }

        .report-date {
            text-align: right;
            color: #95a5a6;
            font-size: 10pt;
            margin-top: 10px;
        }

        /* Módulos */
        .modulo {
            margin-bottom: 40px;
        }

        .modulo-title {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            padding: 15px 20px;
            font-size: 16pt;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 25px;
            border-radius: 5px;
        }

        /* Secciones */
        .seccion {
            margin-bottom: 30px;
        }

        .seccion-title {
            background: #ecf0f1;
            border-left: 4px solid #3498db;
            padding: 12px 20px;
            font-size: 14pt;
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 20px;
        }

        /* Apartados */
        .apartado {
            margin-bottom: 20px;
        }

        .apartado-title {
            font-size: 12pt;
            font-weight: 600;
            color: #34495e;
            margin-bottom: 10px;
            padding-bottom: 5px;
            border-bottom: 1px solid #bdc3c7;
        }

        /* Bloques de contenido */
        .bloque {
            margin-bottom: 15px;
            padding-left: 15px;
        }

        .bloque-texto {
            text-align: justify;
            line-height: 1.5;
        }

        .bloque-lista {
            list-style: none;
        }

        .bloque-lista li {
            padding: 8px 0;
            border-bottom: 1px solid #ecf0f1;
            display: flex;
            justify-content: space-between;
        }

        .bloque-lista li:last-child {
            border-bottom: none;
        }

        .lista-label {
            font-weight: 600;
            color: #2c3e50;
            min-width: 120px;
        }

        .lista-value {
            flex: 1;
            text-align: right;
            color: #7f8c8d;
        }

        /* Experiencias y formaciones */
        .experiencia-item, .formacion-item {
            background: #f8f9fa;
            border-left: 3px solid #3498db;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 0 5px 5px 0;
            page-break-inside: avoid;
        }

        .item-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 8px;
        }

        .item-title {
            font-weight: 600;
            color: #2c3e50;
            font-size: 12pt;
        }

        .item-company {
            color: #7f8c8d;
            font-size: 10pt;
        }

        .item-dates {
            color: #95a5a6;
            font-size: 9pt;
            white-space: nowrap;
        }

        .item-description {
            color: #34495e;
            font-size: 10pt;
            margin-top: 8px;
        }

        /* Competencias */
        .competencia-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 15px;
            background: #f8f9fa;
            margin-bottom: 10px;
            border-radius: 5px;
            page-break-inside: avoid;
        }

        .competencia-name {
            font-weight: 600;
            color: #2c3e50;
        }

        .competencia-score {
            background: #3498db;
            color: white;
            padding: 4px 12px;
            border-radius: 15px;
            font-size: 10pt;
            font-weight: 600;
        }

        /* Puntuación general */
        .puntuacion-general {
            text-align: center;
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            color: white;
            padding: 20px;
            border-radius: 10px;
            margin: 20px 0;
        }

        .puntuacion-numero {
            font-size: 36pt;
            font-weight: 700;
            display: block;
        }

        .puntuacion-texto {
            font-size: 12pt;
            margin-top: 5px;
        }

        /* Contexto de página para saltos */
        .page-context {
            position: fixed;
            top: -1.5cm;
            left: 0;
            right: 0;
            background: #34495e;
            color: white;
            padding: 8px 20px;
            font-size: 9pt;
            z-index: 1000;
            display: none;
        }

        /* Control inteligente de saltos de página */
        .modulo-title {
            page-break-after: avoid;
        }

        .seccion-title {
            page-break-after: avoid;
        }

        .apartado-title {
            page-break-after: avoid;
        }

        /* Prevenir títulos huérfanos */
        .modulo-title + .seccion {
            page-break-before: avoid;
        }

        .seccion-title + .apartado {
            page-break-before: avoid;
        }

        .apartado-title + .bloque {
            page-break-before: avoid;
        }

        /* Mejorar continuidad visual */
        .continuation-header {
            background: #ecf0f1;
            border: 1px solid #bdc3c7;
            padding: 8px 15px;
            font-size: 10pt;
            color: #7f8c8d;
            margin-bottom: 15px;
            border-radius: 3px;
            font-style: italic;
        }

        /* Utilidades */
        .text-center { text-align: center; }
        .text-right { text-align: right; }
        .mb-small { margin-bottom: 10px; }
        .mb-medium { margin-bottom: 20px; }
        .mb-large { margin-bottom: 30px; }

        /* Responsive para contenido muy largo */
        @media print {
            .modulo {
                page-break-inside: avoid;
            }
            
            .seccion {
                page-break-inside: avoid;
            }
            
            .apartado {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <!-- Header del documento -->
    <div class="header">
        <div class="logo-area">
            <div class="company-name">SELECCIÓN & TALENTO</div>
            <div class="report-title">Informe de Evaluación de Candidato</div>
        </div>
        <div class="report-date">
            Fecha: {{fechaPublicacion}}
        </div>
    </div>

    <!-- MÓDULO 1: INFORMACIÓN DEL CANDIDATO -->
    <div class="modulo">
        <h1 class="modulo-title">Información del Candidato</h1>
        
        <!-- Sección: Datos Personales -->
        <div class="seccion">
            <h2 class="seccion-title">Datos Personales</h2>
            
            <div class="apartado">
                <h3 class="apartado-title">Identificación</h3>
                <div class="bloque">
                    <ul class="bloque-lista">
                        <li>
                            <span class="lista-label">Nombre completo:</span>
                            <span class="lista-value">{{datosPersonales.nombreCompleto}}</span>
                        </li>
                        {{#if datosPersonales.nif}}
                        <li>
                            <span class="lista-label">NIF:</span>
                            <span class="lista-value">{{datosPersonales.nif}}</span>
                        </li>
                        {{/if}}
                        {{#if datosPersonales.edad}}
                        <li>
                            <span class="lista-label">Edad:</span>
                            <span class="lista-value">{{datosPersonales.edad}} años</span>
                        </li>
                        {{/if}}
                    </ul>
                </div>
            </div>

            {{#if datosPersonales.direccionTexto}}
            <div class="apartado">
                <h3 class="apartado-title">Dirección</h3>
                <div class="bloque">
                    <ul class="bloque-lista">
                        <li>
                            <span class="lista-label">Dirección:</span>
                            <span class="lista-value">{{datosPersonales.direccionTexto}}</span>
                        </li>
                        {{#if datosPersonales.municipio}}
                        <li>
                            <span class="lista-label">Municipio:</span>
                            <span class="lista-value">{{datosPersonales.municipio}}</span>
                        </li>
                        {{/if}}
                        {{#if datosPersonales.pais}}
                        <li>
                            <span class="lista-label">País:</span>
                            <span class="lista-value">{{datosPersonales.pais}}</span>
                        </li>
                        {{/if}}
                    </ul>
                </div>
            </div>
            {{/if}}

            <div class="apartado">
                <h3 class="apartado-title">Contacto</h3>
                <div class="bloque">
                    <ul class="bloque-lista">
                        {{#if datosPersonales.email}}
                        <li>
                            <span class="lista-label">Email:</span>
                            <span class="lista-value">{{datosPersonales.email}}</span>
                        </li>
                        {{/if}}
                        {{#if datosPersonales.telefonoMovil}}
                        <li>
                            <span class="lista-label">Teléfono:</span>
                            <span class="lista-value">{{datosPersonales.telefonoMovil}}</span>
                        </li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- MÓDULO 2: EVALUACIÓN PROFESIONAL -->
    <div class="modulo">
        <h1 class="modulo-title">Evaluación Profesional</h1>
        
        {{#if informe.puntuacion}}
        <div class="puntuacion-general">
            <span class="puntuacion-numero">{{informe.puntuacion}}</span>
            <div class="puntuacion-texto">Puntuación Global</div>
        </div>
        {{/if}}

        <!-- Sección: Valoración General -->
        {{#if informe.valoracion}}
        <div class="seccion">
            <h2 class="seccion-title">Valoración General</h2>
            <div class="apartado">
                <div class="bloque">
                    <p class="bloque-texto">{{informe.valoracion}}</p>
                </div>
            </div>
        </div>
        {{/if}}

        <!-- Sección: Aspectos Evaluados -->
        <div class="seccion">
            <h2 class="seccion-title">Aspectos Evaluados</h2>
            
            {{#if informe.aspectosPersonales}}
            <div class="apartado">
                <h3 class="apartado-title">Aspectos Personales</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.aspectosPersonales}}</p>
                </div>
            </div>
            {{/if}}

            {{#if informe.entrevistaPersonal}}
            <div class="apartado">
                <h3 class="apartado-title">Entrevista Personal</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.entrevistaPersonal}}</p>
                </div>
            </div>
            {{/if}}

            {{#if informe.conocimientos}}
            <div class="apartado">
                <h3 class="apartado-title">Conocimientos</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.conocimientos}}</p>
                </div>
            </div>
            {{/if}}

            {{#if informe.adecuacion}}
            <div class="apartado">
                <h3 class="apartado-title">Adecuación al Puesto</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.adecuacion}}</p>
                </div>
            </div>
            {{/if}}

            {{#if informe.motivacion}}
            <div class="apartado">
                <h3 class="apartado-title">Motivación</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.motivacion}}</p>
                </div>
            </div>
            {{/if}}

            {{#if informe.potencial}}
            <div class="apartado">
                <h3 class="apartado-title">Potencial</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.potencial}}</p>
                </div>
            </div>
            {{/if}}
        </div>
    </div>

    <!-- MÓDULO 3: HISTORIAL PROFESIONAL -->
    {{#if experienciasLaborales}}
    <div class="modulo">
        <h1 class="modulo-title">Historial Profesional</h1>
        
        <div class="seccion">
            <h2 class="seccion-title">Experiencias Laborales</h2>
            
            <div class="apartado">
                <h3 class="apartado-title">Trayectoria Profesional</h3>
                {{#if informe.trayectoriaProfesional}}
                <div class="bloque">
                    <p class="bloque-texto">{{informe.trayectoriaProfesional}}</p>
                </div>
                {{/if}}
                
                <div class="bloque">
                    {{#each experienciasLaborales}}
                    <div class="experiencia-item">
                        <div class="item-header">
                            <div>
                                <div class="item-title">{{puesto}}</div>
                                <div class="item-company">{{empresa}}</div>
                            </div>
                            <div class="item-dates">
                                {{fechaInicio}} - {{#if fechaFin}}{{fechaFin}}{{else}}Actualidad{{/if}}
                            </div>
                        </div>
                        {{#if funcionRealizada}}
                        <div class="item-description">{{funcionRealizada}}</div>
                        {{/if}}
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
    {{/if}}

    <!-- MÓDULO 4: FORMACIÓN -->
    {{#if formaciones}}
    <div class="modulo">
        <h1 class="modulo-title">Formación</h1>
        
        <div class="seccion">
            <h2 class="seccion-title">Trayectoria Formativa</h2>
            
            <div class="apartado">
                <h3 class="apartado-title">Formación Académica</h3>
                {{#if informe.trayectoriaFormativa}}
                <div class="bloque">
                    <p class="bloque-texto">{{informe.trayectoriaFormativa}}</p>
                </div>
                {{/if}}
                
                <div class="bloque">
                    {{#each formaciones}}
                    <div class="formacion-item">
                        <div class="item-header">
                            <div>
                                <div class="item-title">{{nombre}}</div>
                                <div class="item-company">{{centro}}</div>
                            </div>
                            {{#if fechaFin}}
                            <div class="item-dates">{{fechaFin}}</div>
                            {{/if}}
                        </div>
                        {{#if titulacionAcademica}}
                        <div class="item-description">{{titulacionAcademica}}</div>
                        {{/if}}
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
    {{/if}}

    <!-- MÓDULO 5: COMPETENCIAS Y HABILIDADES -->
    {{#if competencias}}
    <div class="modulo">
        <h1 class="modulo-title">Competencias y Habilidades</h1>
        
        <div class="seccion">
            <h2 class="seccion-title">Evaluación de Competencias</h2>
            
            <div class="apartado">
                <h3 class="apartado-title">Competencias Técnicas y Personales</h3>
                <div class="bloque">
                    {{#each competencias}}
                    <div class="competencia-item">
                        <span class="competencia-name">{{competencia}}</span>
                        <span class="competencia-score">{{puntuacion}}</span>
                    </div>
                    {{#if observaciones}}
                    <div class="bloque-texto mb-small">{{observaciones}}</div>
                    {{/if}}
                    {{/each}}
                </div>
            </div>
        </div>

        {{#if idiomas}}
        <div class="seccion">
            <h2 class="seccion-title">Idiomas</h2>
            <div class="apartado">
                <h3 class="apartado-title">Nivel de Idiomas</h3>
                <div class="bloque">
                    {{#each idiomas}}
                    <div class="competencia-item">
                        <span class="competencia-name">{{idioma}}</span>
                        <span class="competencia-score">{{nivel}}</span>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
        {{/if}}

        {{#if aplicacionesInformaticas}}
        <div class="seccion">
            <h2 class="seccion-title">Aplicaciones Informáticas</h2>
            <div class="apartado">
                <h3 class="apartado-title">Conocimientos Informáticos</h3>
                <div class="bloque">
                    {{#each aplicacionesInformaticas}}
                    <div class="competencia-item">
                        <span class="competencia-name">{{aplicacion}}</span>
                        <span class="competencia-score">{{nivel}}</span>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
        {{/if}}
    </div>
    {{/if}}

    <!-- MÓDULO 6: REFERENCIAS Y CONCLUSIONES -->
    <div class="modulo">
        <h1 class="modulo-title">Referencias y Conclusiones</h1>
        
        {{#if referencias}}
        <div class="seccion">
            <h2 class="seccion-title">Referencias Profesionales</h2>
            <div class="apartado">
                <h3 class="apartado-title">Contactos de Referencia</h3>
                <div class="bloque">
                    {{#each referencias}}
                    <div class="experiencia-item">
                        <div class="item-header">
                            <div>
                                <div class="item-title">{{referencia}}</div>
                                <div class="item-company">{{empresa}}</div>
                            </div>
                        </div>
                        {{#if funcionRealizada}}
                        <div class="item-description">Función: {{funcionRealizada}}</div>
                        {{/if}}
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
        {{/if}}

        {{#if informe.motivoPresentacion}}
        <div class="seccion">
            <h2 class="seccion-title">Conclusiones</h2>
            <div class="apartado">
                <h3 class="apartado-title">Motivo de Presentación</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.motivoPresentacion}}</p>
                </div>
            </div>
            {{#if informe.datosInteres}}
            <div class="apartado">
                <h3 class="apartado-title">Datos de Interés</h3>
                <div class="bloque">
                    <p class="bloque-texto">{{informe.datosInteres}}</p>
                </div>
            </div>
            {{/if}}
        </div>
        {{/if}}
    </div>

</body>
</html>